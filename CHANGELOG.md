# Changelog

## 1.0.6

- 统一所有 UI 入口使用 `icon.svg` 主视觉。
- 重新设计浏览器工具栏 Popup，移除生硬的白边按钮，改为更轻量的操作区。

## 1.0.5

- 树节点标题和正文改为按节点宽度截断并显示省略号，修复长文本溢出节点框的问题。

## 1.0.4

- 优化树面板中的 JSON、Markdown、SVG 导出按钮样式。
- 为浏览器工具栏 Popup 增加黑夜模式适配。

## 1.0.3

- 修复 `injected.js` 在 `document_start` 阶段未真正加载，导致 ChatGPT API 无法捕获的问题。
- 移除会产生 404 噪声的手动 conversation 请求，刷新动作改为重新加载页面。

## 1.0.2

- 修复拖拽树图后 React 读取空拖拽状态导致面板崩溃的问题。
- 为面板增加错误边界，避免后续局部异常导致整个插件界面消失。

## 1.0.1

- 修复 ChatGPT 黑夜模式识别不准确的问题。
- 修复点击“放大”按钮后树图可视范围异常的问题，并增加缩放边界。

## 1.0.0

- 完成 Manifest V3 扩展骨架。
- 支持捕获 ChatGPT conversation API 并解析完整 mapping。
- 支持树状图、缩放、平移、搜索、角色筛选和活跃路径过滤。
- 支持点击节点定位原消息，以及版本分支切换。
- 支持 JSON、Markdown、SVG 导出和节点内容复制。
- 增加本地存储版本标记与旧对话自动清理。

## 0.5.0

- 增加全文搜索、角色筛选和活跃路径过滤。
- 增加 JSON、Markdown、SVG 导出。

## 0.3.0

- 增加树节点到 ChatGPT 消息的定位。
- 增加版本分支切换与高亮反馈。

## 0.2.0

- 增加可交互树状图面板。

## 0.1.0

- 建立仓库与基础规划。
